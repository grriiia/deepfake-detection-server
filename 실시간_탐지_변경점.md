# 실시간 탐지 변경점

WebSocket 기반의 스트리밍 아키텍처를 사용하여 실시간 딥페이크 탐지 기능을 성공적으로 구현했습니다. 이제 사용자는 웹캠이나 화면 공유를 통해 즉각적인 분석 피드백을 받을 수 있습니다.

## 주요 변경 사항

### 1. 백엔드 (Backend)
- **`deepfake_service.py`**: 단일 프레임(바이너리 데이터)을 직접 추론하기 위한 `predict_frame(frame_bytes)` 함수를 추가했습니다.
- **`app/api/live_deepfake.py`**: `/ws/live-deepfake` WebSocket 엔드포인트를 신규 생성했습니다.
    - JPEG 프레임을 바이너리 형태로 수신합니다.
    - **지수 이동 평균(EMA)** 알고리즘을 적용하여 신뢰도 점수의 급격한 변동을 억제하고 안정적인 UI 경험을 제공합니다.
    - 원본 점수와 안정화된 최종 판정 결과를 JSON으로 전송합니다.
- **`app/main.py`**: `/ws` 경로 접두사 아래에 새로운 WebSocket 라우터를 등록했습니다.

### 2. 프론트엔드 (Frontend)
- **`LiveDetect.tsx`**: 실시간 분석을 위한 고기능 신규 페이지입니다.
    - **소스 선택**: 웹캠과 화면 공유(Screen Capture) 모드 간 전환이 가능합니다.
    - **실시간 HUD**: 현재 판정 결과, 분석된 프레임 수, 통신 상태 등을 오버레이로 표시합니다.
    - **실시간 분석 그래프**: 현재 신뢰도 게이지와 시간에 따른 점수 변화를 보여주는 히스토리 그래프를 포함합니다.
    - **리소스 자동 정리**: 페이지 이탈 시 카메라 권한 반납 및 WebSocket 연결 종료를 자동으로 수행합니다.
- **`routes.tsx`**: 애플리케이션 라우팅에 `/live` 경로를 추가했습니다.
- **`Analyze.tsx`**: 기존 "카메라" 탭 클릭 시 실시간 판별 페이지로 바로 이동할 수 있도록 카드 UI로 교체했습니다.
- **`Navigation.tsx`**: 상단 네비게이션 바에 "실시간 판별" 전용 버튼을 추가하여 접근성을 높였습니다.

### 3. 검증 및 상태
- **빌드 성공**: 프론트엔드 프로덕션 빌드(`npm run build`)가 오류 없이 완료되었습니다.
- **아키텍처 준수**: 기존의 디자인 시스템(Tailwind CSS) 및 백엔드 구조(FastAPI Router)와 일관성을 유지하며 구현되었습니다.
- **구현 완료 기능**:
    - [V] 웹캠 스트림 캡처 및 전송
    - [V] 화면 공유 캡처 및 전송
    - [V] 저지연 WebSocket 바이너리 스트리밍
    - [V] 프레임 단위 백엔드 추론
    - [V] 신뢰도 점수 안정화 알고리즘 (EMA)
    - [V] 실시간 판정 UI 오버레이 및 시각화

---

## 추가 업데이트 (2026-05-23)

### 실시간 탐지 병목 현상 개선 (큐 제어 및 프레임 드롭 기법 도입)
- **배경**: AI 모델(EfficientNet-B4)의 추론 속도 지연으로 인해 프레임이 백엔드 내부 TCP/WebSocket 버퍼 및 이벤트 루프 상에 누적되면서 실시간 분석의 싱크가 뒤처지는 병목 현상이 발생했습니다.
- **수정 사항**:
  - 백엔드 WebSocket 라우터(`live_deepfake.py`) 내에 최대 1개의 크기를 갖는 `asyncio.Queue` 및 `asyncio.Event` 구조를 도입했습니다.
  - **프레임 드롭 메커니즘**: 클라이언트로부터 지속적으로 프레임을 받되, 큐가 차 있을 경우 기존 대기 프레임을 꺼내 버리고(`get_nowait()`) 가장 최신 프레임만 대기열에 유지하도록 하였습니다.
  - **비동기 태스크 분리**: 수신 태스크(`receive_loop`)와 추론 워커 태스크(`worker_loop`)를 별도의 비동기 태스크로 분리 실행하여 상호 블로킹을 차단하고 리소스를 안전하게 관리합니다.
- **결과**: 다중 프레임 폭주 시에도 누적 지연 없이 가장 최신의 프레임만 실시간으로 판정하여 지연 오버헤드를 해소했습니다.
