데스크탑에서 할 일
- cmd에서 ipconfig 를 입력하여 adapter 이더넷의 IPv4를 확인할 것
- 백엔드는 "uvicorn app.main:app --host 0.0.0.0 --port 8000" 를 사용할 것
- 이후 "http://localhost:8000/health" 정상 응답 확인
- Windows 방화벽에서 python.exe 허용 확인

개인 네트워크 체크
공용 네트워크 체크

노트북에서 할 일
- 프론트엔드만 실행해도 됨
- "http://데스크탑IP:8000/health" 이걸로 접속 테스트
- 성공하면 "npm run dev -- --host" 실행
- 노트북의 프론트 코드에서 localhost로 되어 있는 것을 모두 데스크탑IP로 변경
-- client.ts, Login.tsx, LiveDetect.tsx
- 중요한 점이 "ping 데스크탑IP"이거를 통해 동일 IP인지 확인 필수
